# @acme/{{ kebabCase name }}

Feature package scaffolded by `pnpm turbo gen feature`.

Included in this build:
{{#if api}}- ✅ tRPC API router + server context (sample `items` entity){{else}}- ❌ API router{{/if}}
{{#if react}}- ✅ React UI (components, hooks, client provider){{else}}- ❌ React UI (backend-only — React ESLint/deps disabled){{/if}}
{{#if backendTests}}- ✅ Backend tests (testcontainers + caller){{else}}- ❌ Backend tests{{/if}}
{{#if frontendTests}}- ✅ Frontend tests (msw-trpc){{else}}- ❌ Frontend tests{{/if}}

## Layout

```
src/
{{#if api}}  api/
    trpc.ts                 # per-feature tRPC context + middleware (auth, rate limit, telemetry, db)
    root.ts                 # appRouter — aggregates routers
    routers/{{ kebabCase name }}.ts        # sample CRUD router
    schemas/item-schema.ts  # drizzle table + zod schemas
  env.ts                    # type-safe env (@t3-oss/env-nextjs)
{{/if}}{{#if react}}  components/{{ kebabCase name }}-list.tsx # sample UI (presentational)
  hooks/use-{{ kebabCase name }}.ts        # data-access hook (tRPC + React Query)
  trpc/                     # client (react.tsx), RSC (server.tsx), query-client
{{/if}}  index.ts{{#if api}} | index-server.ts | index-schema.ts{{/if}}   # public entrypoints
{{#if backendTests}}  tests/backend/            # setup, global-setup (testcontainers), utils, router tests
{{/if}}{{#if frontendTests}}  tests/frontend/           # setup (renderWithProviders + trpcMsw), then per ADR 0018:
    unit/                   #   pure logic, no React tree
    integration/hooks/      #   hook via real QueryClient + MSW (the contract)
    integration/components/ #   component through its providers, assert DOM
{{/if}}```
{{#if anyTests}}
## Testing

Run with `pnpm -F @acme/{{ kebabCase name }} test`.
{{#if backendTests}}
### Backend (`test:backend`) — real DB, real Redis

Uses **testcontainers** (via `@acme/test-utils/setup`) so procedures run against
throwaway PostgreSQL + Redis containers. **Docker/Podman must be running.**

- Build a caller with `createTestContext(...)`, then call
  `appRouter.createCaller(ctx).{{ camelCase name }}.<procedure>(...)`.
- Mock only what you don't own (Clerk auth, telemetry); exercise real persistence.
- Test shared middleware (auth, rate limit) **once**; cover business logic with the
  **zero / one / many** pattern; seed data with `fixtures.ts`.
{{/if}}{{#if frontendTests}}
### Frontend (`test:frontend`) — MSW at the HTTP boundary, no DB

Doctrine: [ADR 0018](../../../docs/adr/0018-frontend-test-doctrine.md) — **fake the
network at the HTTP boundary (msw-trpc + `setupServer`), assert what renders.**
Never `vi.mock` the tRPC client, a feature hook, or `react-toastify`; assert the
observable outcome (DOM / returned hook state), never `expect(mock).toHaveBeenCalled`.
The provider switches to a plain `httpLink` when `NODE_ENV==='test'` so MSW can
intercept.

```ts
// integration/components/{{ kebabCase name }}-list.test.tsx
server.use(trpcMsw.{{ camelCase name }}.list.query(() => [/* rows */]));
renderWithProviders(<{{ pascalCase name }}List />);

// integration/hooks/use-{{ kebabCase name }}.test.tsx — the hook is the contract
const { result } = renderHook(() => use{{ pascalCase name }}(), { wrapper: Providers });
```
{{/if}}{{/if}}
{{#if api}}
## Wire it into an app (manual steps the generator can't do)

1. Add the dependency to the app: `"@acme/{{ kebabCase name }}": "workspace:*"`.
2. Add the API route handler:

   ```ts
   // apps/nextjs/src/app/api/trpc/{{ kebabCase name }}/[trpc]/route.ts
   import { fetchRequestHandler } from '@trpc/server/adapters/fetch';

   import { appRouter, createTRPCContext } from '@acme/{{ kebabCase name }}/server';

   const handler = (req: Request) =>
     fetchRequestHandler({
       endpoint: '/api/trpc/{{ kebabCase name }}',
       req,
       router: appRouter,
       createContext: () => createTRPCContext({ headers: req.headers }),
     });

   export { handler as GET, handler as POST };
   ```
{{#if react}}
3. Wrap the relevant tree with `{{ pascalCase name }}TRPCReactProvider` (from `@acme/{{ kebabCase name }}`).
{{/if}}
4. Register the schema for migrations (drizzle config `schema` glob), then `pnpm db:push`.
{{/if}}