// Database schema exports — safe to import in any context (CLI, server, client
// build). Intentionally has NO `server-only` guard so drizzle-kit can load it.
export { items } from './api/schemas/item-schema';
