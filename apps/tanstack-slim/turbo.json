{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tags": ["composition"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".nitro/**", ".output/**", ".tanstack/**"]
    },
    "dev": {
      "persistent": true
    }
  }
}
